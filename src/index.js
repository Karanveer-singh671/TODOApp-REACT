import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
// renders app component into the div id root in index.html
ReactDOM.render(<App />, document.getElementById('root'));

